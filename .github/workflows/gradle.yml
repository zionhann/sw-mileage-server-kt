# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

permissions:
  contents: read
  issues: read
  checks: write
  pull-requests: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: Configure the properties
      run: |
        echo "${{ secrets.APP_CONFIG }}" > ./src/main/resources/application.yml
    - name: Build with Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: 8.2.1
        arguments: build
    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v2.9.0
      if: always()
      with:
        files: |
          build/test-results/**/*.xml
    - name: Deploy to WALAB/sw_mileage
      run: |
        curl -X POST https://f073-218-150-69-241.ngrok-free.app/job/MileageSystem/build --user yujin:1151457c2aeb13316c179ea2ce64e6439f
